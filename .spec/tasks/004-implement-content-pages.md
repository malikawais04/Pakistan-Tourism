# Task 004: Implement Content Pages

## Status
Completed

## Priority
High

## Description
Build the remaining content pages: destinations & experiences listings (search + filter), destination/experience detail pages with galleries, About, and Contact — satisfying the "minimum 5 pages" requirement.

## Acceptance Criteria
- [x] `/destinations` and `/experiences` — searchable, filterable listing pages (`components/Listing.tsx`)
- [x] `/destinations/[slug]` and `/experiences/[slug]` — detail pages with hero, facts, gallery + lightbox, related notes (`components/Detail.tsx`, `components/Lightbox.tsx`)
- [x] `/about` — mission/approach content with an "Ask the guide" entry point
- [x] `/contact` — client-side form with validation and a local success state (`components/ContactForm.tsx`)
- [x] All dynamic routes pre-rendered via `generateStaticParams`

## Time Estimation
4 hours

# Task 001: Project Setup

## Status
Completed

## Priority
High

## Description
Initialize the Next.js 14 project with TypeScript, Tailwind CSS, and the App Router, migrated from the original Vite + React + Express codebase. Port the design tokens and global stylesheet with no visual regression.

## Acceptance Criteria
- [x] Next.js project configured (`package.json`, `next.config.mjs`, `tsconfig.json`)
- [x] TypeScript strict mode configured
- [x] Tailwind CSS + PostCSS configured, `app/globals.css` ported from the legacy `index.css`
- [x] `next/font` wired for DM Serif Display, Manrope, and IBM Plex Mono
- [x] `.gitignore` covers `node_modules`, `.next`, `.env*`, `backend/.venv`

## Time Estimation
2 hours
